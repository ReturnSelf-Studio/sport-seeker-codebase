name: sport_seeker
description: Sport Seeker Desktop App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  file_picker: ^10.3.10
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  path_provider: ^2.1.5
  dio: ^5.9.2
  shared_preferences: ^2.5.5
  window_manager: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/backend/api_payload.tar.gz
    - assets/icons/app_icon.png
