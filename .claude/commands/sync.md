# /sync — Copy files từ minimal sang main

Chạy script sync để copy tất cả files đã thay đổi từ `codebase-minimal` sang `codebase-main`.

```bash
bash /Users/arlix/projects/AIBus-SportSeeker/codebase-minimal/.claude/sync.sh
```

Sau khi sync xong, confirm với user trước khi build.
