# requirements-windows.txt
# Windows (x86_64) — Python 3.11
# Install: uv pip install -r requirements-windows.txt

# ============================================================
# PRE-INSTALL: Insightface
# Khong co wheel chinh thuc tren PyPI cho Windows → dung prebuilt
# tu Gourieff/Assets (tin cay, duoc dung rong rai boi cong dong ComfyUI)
# Khai bao TRUOC de pip/uv resolve dung dependency
# ============================================================
insightface @ https://github.com/Gourieff/Assets/raw/main/Insightface/insightface-0.7.3-cp311-cp311-win_amd64.whl

# ============================================================
# CHI DUNG PRE-BUILT WHEEL — khong compile from source
# Tranh loi "Microsoft Visual C++ required" tren may khach
# ============================================================
--only-binary numpy
--only-binary opencv-python
--only-binary pyarrow
--only-binary faiss-cpu
--only-binary rapidocr-onnxruntime

# ============================================================
# TORCH — CPU-only build
# Phải khai báo TRƯỚC sentence-transformers để uv/pip resolve đúng.
# sentence-transformers kéo torch như transitive dep — nếu không pin,
# pip tự chọn CUDA build → c10.dll crash trên máy không có NVIDIA GPU.
# CPU build hoạt động trên mọi máy Windows (Intel/AMD, có hoặc không GPU).
# ============================================================
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# ============================================================
# CORE
# ============================================================
numpy>=1.24,<2.0.0
opencv-python>=4.8
pyarrow>=17.0.0
pandas>=2.0.3

# ============================================================
# FACE RECOGNITION
# onnxruntime-directml: GPU acceleration qua DirectML (AMD/Intel/NVIDIA),
# auto-fallback CPU nếu không có GPU tương thích.
# ============================================================
onnxruntime-directml>=1.16

# ============================================================
# FAISS — vector search
# ============================================================
faiss-cpu>=1.7.4

# ============================================================
# OCR
# rapidocr-onnxruntime reuse onnxruntime-directml, auto GPU/CPU detect.
# Thay the paddleocr de tranh loi OneDNN incompatibility tren Windows.
# ============================================================
rapidocr-onnxruntime>=1.4.0

# ============================================================
# TEXT EMBEDDINGS
# sentence-transformers sẽ dùng torch CPU build đã pin ở trên.
# ============================================================
sentence-transformers>=2.2.2

# ============================================================
# BACKEND API
# ============================================================
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0.3
python-multipart>=0.0.6

# ============================================================
# SYSTEM UTILS
# ============================================================
psutil>=5.9.0
